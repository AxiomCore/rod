---
title: Enums
description: Validating against a fixed set of string values.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

# Enums

Enums validate that a string matches one of the allowed values in a predefined list.

<Tabs items={['Rust', 'TypeScript', 'Python']}>
  <Tab value="Rust">
    ```rust
    use rod_rs::enum_type;
    
    let schema = enum_type(vec!["admin", "user", "guest"]);
    ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript
    import { rod } from 'rod-js';
    
    const schema = rod.enum("admin", "user", "guest");
    ```
  </Tab>
  <Tab value="Python">
    ```python
    import rod
    
    schema = rod.enum("admin", "user", "guest")
    ```
  </Tab>
</Tabs>

## Performance

Internally, Rod optimizes enums by checking the input string against the allowed list directly, bypassing the overhead of standard union trial-and-error.